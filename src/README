Start the autosub daemon:

./autosub.sh start <configfile>

with <configfile> being the path to the to be used config file.

Stop the autosub daemon:

./autosub.sh stop

==============

More Information can be found in the usermanual located in doc/doc_pdf/usermanual.pdf
