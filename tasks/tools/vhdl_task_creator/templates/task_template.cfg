task_name="{{ task_name }}"
userfiles="{{ userfiles }}"
entityfiles="{{ entityfiles }}"
extrafiles="{{ extrafiles }}"
constraintfile="{{ constraintfile }}"

simulation_timeout="{{ simulation_timeout }}s"
attach_wave_file="{{ attach_wave_file }}" # "1" to attach, "0" to not attach